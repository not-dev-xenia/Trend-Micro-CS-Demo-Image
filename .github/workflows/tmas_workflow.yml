name: Publish Docker image

on: 
  push:
    branches: 
      - master
env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  TMAS_API_KEY: ${{ secrets.TMAS_API_KEY }} 
jobs:
  push_to_registries:
    name: Push Docker image to multiple registries
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Check if secret exists
        run: |
          if [ -z "${MY_SECRET}" ]; then
            echo "Secret MY_SECRET NO existe o está vacío"
            exit 1
          else
            echo "Secret MY_SECRET está configurado"
          fi
        env:
          MY_SECRET: ${{ secrets.TMAS_API_KEY }}
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@main
      - name: 'Login to GitHub Container Registry'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@9ec57ed1fcdbf14dcef7dfbe97b2010124a938b7
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
      - name: Build and push Docker image
        uses: docker/build-push-action@f2a1d5e99d037542a71f64918e516c093c6f3fc4
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      - name: Download TMAS and Scan Repo for Open Source Vulnerabilities and Secrets - DIR Scan
        uses: trendmicro/tmas-scan-action@v3.0.1
        with:
          version: '2' # Recommended: pin to major version for automatic updates within v2.x.x
          vulnerabilitiesScan: true
          malwareScan: false
          secretsScan: true
          artifact: dir:./
          additionalArgs: --region=us-east-1
          tmasApiKey: ${{ env.TMAS_API_KEY }}
          githubToken: ${{ secrets.GITHUB_TOKEN }}
        env:
          REGISTRY: ghcr.io
          IMAGE_NAME: ${{ github.repository }}
          TMAS_API_KEY: ${{ env.TMAS_API_KEY }}
      - name: Download TMAS and Scan Repo for Open Source Vulnerabilities and Secrets
        uses: trendmicro/tmas-scan-action@v3.0.1
        with:
          version: '2' # Recommended: pin to major version for automatic updates within v2.x.x
          vulnerabilitiesScan: true
          malwareScan: false
          secretsScan: true
          artifact: registry:${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.tags }}
          additionalArgs: --region=us-east-1
          tmasApiKey: ${{ env.TMAS_API_KEY }}
          githubToken: ${{ secrets.GITHUB_TOKEN }}
        env:
          REGISTRY: ghcr.io
          IMAGE_NAME: ${{ github.repository }}
          TMAS_API_KEY: ${{ env.TMAS_API_KEY }} 
        
      - name: 'Upload Scan Result Artifact'
        uses: actions/upload-artifact@v4
        with:
          name: scan-result
          path: result.json
          retention-days: 30
